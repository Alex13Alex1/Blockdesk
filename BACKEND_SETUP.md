# 🚀 Настройка бекенда для управления визуализацией

## Быстрый старт

### 1. Перейдите в папку backend

```bash
cd backend
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Запустите сервер

```bash
npm start
```

Или для разработки с автоперезагрузкой:

```bash
npm run dev
```

### 4. Откройте админ-панель

В браузере откройте: **http://localhost:3001/admin**

## 📡 API Endpoints

- **GET** `http://localhost:3001/api/visualization` - Получить текущую визуализацию
- **POST** `http://localhost:3001/api/visualization` - Сохранить визуализацию

## 🎨 Использование админ-панели

1. Откройте http://localhost:3001/admin в браузере
2. Включите/выключите визуализацию чекбоксом
3. Вставьте SVG или HTML код в текстовое поле
4. Нажмите "💾 Save Visualization"
5. Визуализация автоматически появится на фронтенде

## 🔧 Настройка URL бекенда

Если бекенд работает на другом порту или адресе, создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:3001
```

## 📝 Пример SVG для вставки

```svg
<svg viewBox="0 0 1000 600" class="h-auto w-full">
  <rect x="20" y="80" width="200" height="180" rx="16" fill="#3b82f6" opacity="0.3"/>
  <text x="40" y="115" font-size="16" fill="#e0e7ff" font-weight="700">Market Context</text>
  <!-- Добавьте свой SVG код -->
</svg>
```

## ⚠️ Важно

- Бекенд должен быть запущен для работы визуализации
- Если бекенд недоступен, визуализация просто не отобразится (без ошибок)
- Данные сохраняются в файл `backend/visualization.json`
