# Blockdesk Backend - Visualization Management

Backend API для управления визуализацией платформы Blockdesk.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
npm install
```

### 2. Запуск сервера

```bash
npm start
```

Или для разработки с автоперезагрузкой:

```bash
npm run dev
```

### 3. Доступ к админ-панели

Откройте в браузере:
- **Админ-панель**: http://localhost:3001/admin
- **API**: http://localhost:3001/api/visualization

## 📡 API Endpoints

### GET `/api/visualization`
Получить текущую визуализацию.

**Response:**
```json
{
  "enabled": true,
  "content": "<svg>...</svg>",
  "updatedAt": "2024-01-20T15:30:00.000Z"
}
```

### POST `/api/visualization`
Сохранить визуализацию.

**Request Body:**
```json
{
  "enabled": true,
  "content": "<svg>...</svg>"
}
```

**Response:**
```json
{
  "success": true,
  "visualization": {
    "enabled": true,
    "content": "<svg>...</svg>",
    "updatedAt": "2024-01-20T15:30:00.000Z"
  }
}
```

## 🎨 Использование админ-панели

1. Откройте http://localhost:3001/admin
2. Включите/выключите визуализацию чекбоксом
3. Вставьте SVG или HTML код в текстовое поле
4. Нажмите "Save Visualization"
5. Визуализация будет доступна на фронтенде через API

## 📝 Примечания

- Данные сохраняются в файл `visualization.json`
- Сервер работает на порту 3001
- CORS включен для работы с фронтендом
