# 📸 Как добавить изображение на сайт

## ✅ Что уже сделано

1. ✅ Создана папка `public/images/` для изображений
2. ✅ Создан компонент `PlatformOverviewImage` для отображения изображения
3. ✅ Компонент добавлен в `App.tsx` после Hero секции

## 📋 Инструкция по добавлению изображения

### Шаг 1: Подготовьте изображение

1. Убедитесь, что у вас есть файл изображения (PNG, JPG, SVG или WebP)
2. Рекомендуемые форматы:
   - **PNG** — для изображений с прозрачностью
   - **JPG** — для фотографий
   - **SVG** — для векторной графики
   - **WebP** — современный формат (лучшее качество/размер)

### Шаг 2: Переименуйте файл

Переименуйте ваш файл изображения в одно из следующих имен:
- `platform-overview.png` (рекомендуется)
- `platform-overview.jpg`
- `platform-overview.jpeg`
- `platform-overview.svg`
- `platform-overview.webp`

### Шаг 3: Скопируйте файл в папку

Скопируйте файл изображения в папку:
```
public/images/platform-overview.png
```

**Пример:**
- Если ваш файл называется `blockvis-infographic.png`
- Переименуйте его в `platform-overview.png`
- Скопируйте в `C:\Users\alexs\Blockdesk\public\images\platform-overview.png`

### Шаг 4: Проверьте результат

1. Убедитесь, что dev-сервер запущен: `npm run dev`
2. Откройте сайт: http://localhost:5173
3. Прокрутите вниз после Hero секции
4. Изображение должно отобразиться автоматически

## 🎨 Где отображается изображение

Изображение появляется:
- **После Hero секции** (после блока "At a glance")
- **Перед секцией "Platform Overview"** (динамическая визуализация из бекенда)

## 🔧 Если изображение не отображается

### Проверьте:

1. **Путь к файлу:**
   ```
   public/images/platform-overview.png
   ```
   Файл должен быть именно в этой папке с этим именем.

2. **Формат файла:**
   - Компонент автоматически пробует разные форматы (png, jpg, jpeg, svg, webp)
   - Но лучше использовать `.png` для надежности

3. **Размер файла:**
   - Для веба рекомендуется размер файла < 2MB
   - Если файл слишком большой, сожмите его:
     - Онлайн: https://tinypng.com/
     - Или используйте инструменты сжатия изображений

4. **Консоль браузера:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку Console
   - Проверьте ошибки загрузки изображения

## 📝 Альтернативный способ (если нужно другое имя файла)

Если вы хотите использовать другое имя файла, откройте:
```
src/components/PlatformOverviewImage.tsx
```

И измените строку:
```tsx
const imagePath = "/images/platform-overview";
```

На ваше имя файла (без расширения):
```tsx
const imagePath = "/images/ваше-имя-файла";
```

## 🚀 Для продакшена

После добавления изображения:
1. Изображение автоматически будет включено в сборку при `npm run build`
2. Файлы из папки `public/` копируются в `dist/` при сборке
3. На GitHub Pages изображение будет доступно по пути `/Blockdesk/images/platform-overview.png`

## 💡 Советы

- **Оптимизация:** Используйте сжатые изображения для быстрой загрузки
- **Альтернативный текст:** Компонент уже включает `alt` текст для доступности
- **Адаптивность:** Изображение автоматически адаптируется под размер экрана
